name: Run on push
on:
 push:
  branches:
   - main
 workflow_dispatch

jobs:
 diff:
  runs-on: ubuntu-latest
  steps:
    - run: echo this is my first diff
 print-end:
  runs-on: ubuntu-latest
  needs: [diff]
  steps:
    - run: echo this is the end of the first job
